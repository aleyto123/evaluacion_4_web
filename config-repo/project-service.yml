server:
  port: 8082

spring:
  datasource:
    url: ${PROJECT_DB_URL:jdbc:mysql://localhost:3308/projects_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${PROJECT_DB_USERNAME:root}
    password: ${PROJECT_DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,circuitbreakers,retries
  endpoint:
    health:
      show-details: always

resilience4j:
  circuitbreaker:
    instances:
      userService:
        sliding-window-size: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 2
  retry:
    instances:
      userService:
        max-attempts: 3
        wait-duration: 1s

info:
  app:
    name: Project Service
    description: CRUD de proyectos y asignacion de responsables

app:
  message: Servicio de proyectos operativo
  fallback-message: No fue posible consultar el servicio de usuarios. Intente nuevamente.
